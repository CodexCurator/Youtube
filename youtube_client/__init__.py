# This file can be empty, or it can be used to mark the directory as a package
# and contain package-level initialization code.

# For now, let's just ensure the app can be found if we structure it as a package.
# from .app import create_app
#
# if you want to run `flask run` from outside the `youtube_client` directory,
# you might need a `wsgi.py` or to set FLASK_APP=youtube_client.app:create_app
# For simplicity with `python app.py` direct execution, this file being empty is fine.
